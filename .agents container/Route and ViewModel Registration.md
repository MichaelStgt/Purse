# Task:

After I rewiewed the code, I noticed that there are some things that need to be fixed. Please make sure to address the following issues:

1. **The GoBackCommand**: I added a GoBackCommand to handle navigation when the user wants to go back for all of our "Detail" ViewModel. Once the user navigates back to the previous page, the SaveCommand is executed.
I also added the UI (Shell navigation) to trigger this command.
2. **Route registration**: Currently, all Routes and ViewModel